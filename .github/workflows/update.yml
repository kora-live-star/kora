name: Auto Update Matches

on:
  schedule:
    - cron: '*/30 * * * *' # تحديث كل 30 دقيقة
  workflow_dispatch:

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run Scraper Script
        run: python scrape_matches.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Auto Bot"
          git add matches.json streams.json || true
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto Update Matches"
          git push || true